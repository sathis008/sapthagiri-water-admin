import { useState } from 'react';
import { KeyboardAvoidingView, Platform, StyleSheet, View } from 'react-native';
import { Button, HelperText, Text, TextInput } from 'react-native-paper';
import { MaterialCommunityIcons } from '@expo/vector-icons';
import { useDispatch } from 'react-redux';
import { useLoginMutation } from '../services/api'; import { sessionStorage } from '../services/storage'; import { setSession } from '../store';
export default function LoginScreen() { const dispatch = useDispatch(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [login, { isLoading, error }] = useLoginMutation(); const submit = async () => { try { const session = await login({ email: email.trim(), password }).unwrap(); await sessionStorage.set(session); dispatch(setSession(session)); } catch {} }; const message = error && typeof error === 'object' && 'data' in error ? (error.data as { message?: string })?.message : undefined; return <KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : undefined} style={styles.page}><View style={styles.top}><View style={styles.logo}><MaterialCommunityIcons name="water" size={36} color="#0B6E4F"/></View><Text variant="headlineMedium" style={styles.brand}>Sapthagiri Water</Text><Text style={styles.tagline}>Operations made effortless</Text></View><View style={styles.form}><Text variant="headlineSmall" style={styles.welcome}>Welcome back</Text><Text style={styles.helper}>Sign in to manage deliveries and bookings.</Text><TextInput mode="outlined" label="Email address" value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" left={<TextInput.Icon icon="email-outline"/>}/><TextInput mode="outlined" label="Password" value={password} onChangeText={setPassword} secureTextEntry onSubmitEditing={submit} left={<TextInput.Icon icon="lock-outline"/>}/><HelperText type="error" visible={!!message}>{message || ''}</HelperText><Button mode="contained" icon="login" loading={isLoading} disabled={!email || !password || isLoading} onPress={submit} contentStyle={styles.button}>Sign in</Button></View></KeyboardAvoidingView>; }
const styles = StyleSheet.create({ page: { flex: 1, justifyContent: 'center', backgroundColor: '#F4F7F5', padding: 24 }, top: { alignItems:'center', marginBottom:36 }, logo:{height:70,width:70,borderRadius:24,backgroundColor:'#fff',alignItems:'center',justifyContent:'center',marginBottom:15,elevation:2}, brand: { fontWeight: '800', color: '#102019' }, tagline:{color:'#68756E',marginTop:5}, form: { gap: 13, backgroundColor: '#fff', padding: 22, borderRadius: 24, elevation: 2 }, welcome:{fontWeight:'800'},helper:{color:'#68756E',marginBottom:5}, button: { height: 50 } });
